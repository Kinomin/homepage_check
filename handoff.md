# School Insight AI — 実装ハンドオフ

私立中高一貫校の入試広報担当者向け、学校ホームページ分析ツール。
本書は Claude Code での実装を前提とした仕様書。プロトタイプ（`prototype.html`）が UI の正典。

**読む順番：** この文書 → `prototype.html` をブラウザで開いて全画面を操作 → 実装開始。

---

## 1. この製品が答える問い

3つの画面が、それぞれ別の問いに答える。ここが崩れると製品が散る。

| 画面 | 問い |
|---|---|
| 02 欠落マップ | すでにサイトに来た人が、探した情報を見つけられるか |
| 03 導線の実測 | 来た人が説明会の申込にたどり着けるか |
| 04 発見のされ方 | そもそも、まだ学校名を知らない人に届いているか |

05 ペルソナ仮説と 06 改善アクションは、上記3つの**解釈層**。
01 サマリーと 07 レポート出力は**提示層**。

### 設計原則（最重要）

1. **計測と解釈を分離する。** 02・03・04 は機械的な計測結果のみ。LLM の解釈は 05・06 のみ。
2. **すべての指摘に根拠を添える。** 判定に使ったURL・件数を必ず保持し、画面に出す。
3. **比較校を採点しない。** ページの有無・更新日・掲載件数という事実のみ記録。優劣の判定・評価文は生成しない。
4. **取得できなかったことを「情報がない」と表示しない。** 走査失敗と欠落を必ず区別する。

---

## 2. 技術構成

- Frontend: Next.js (App Router) / TypeScript / Tailwind CSS
- Backend: Supabase（Postgres + Auth + Storage）
- クロール: Playwright（レンダリング必要時）+ Cheerio（静的解析）
- 判定: Claude API
- 順位取得: 外部の順位計測 API（**Phase1 では未実装。要検討事項 D 参照**）

環境変数で API キーを管理。UI と分析処理は分離。

---

## 3. データモデル

```
schools            自校・比較校の共通マスタ
  id, name, url, prefecture, school_type, coed_type,
  has_junior_admission, has_senior_admission, has_affiliated_university,
  robots_allowed (bool), created_at

organizations      契約単位（学校法人）
  id, name, plan, created_at

org_schools        organization と school の関係
  org_id, school_id, role ('self' | 'competitor'), sort_order
  ※ competitor は 4〜5校まで

scans              走査の実行単位
  id, school_id, started_at, finished_at,
  status ('queued'|'running'|'done'|'blocked'|'failed'),
  page_count, indexed_count, image_count, pdf_only_count, crawl_depth

pages              走査で取得した個別ページ
  id, scan_id, url, title, meta_description, h1_count,
  word_count, image_count, has_json_ld, json_ld_types[],
  last_modified, http_status, is_pdf

criteria           調査項目マスタ（31項目・固定）
  id ('A1'..'G3'), category, label, audience,
  judge_prompt, applicable_when (例: has_affiliated_university)

findings           項目ごとの判定結果
  id, scan_id, criterion_id,
  level ('full'|'mid'|'thin'|'none'|'n/a'|'unknown'),
  evidence_text, evidence_urls[], evidence_counts (jsonb),
  judged_by ('rule'|'llm'), judged_at

measurements       03 の計測値
  id, scan_id, key, value, unit, method ('scan'|'operate'|'external')

rankings           04 の順位（Phase2）
  id, school_id, keyword, keyword_type ('branded'|'generic'),
  position, top_domain, measured_at

actions            改善アクション
  id, scan_id, action_key, priority, difficulty,
  source_criterion_id, status ('open'|'doing'|'done'|'wontfix'),
  assignee_note, updated_at

action_threads     アクションへの照会（学校 → AI）
  id, action_id, role ('user'|'assistant'), body, created_at

reports            レポート出力
  id, scan_id, included_blocks[], generated_at, pdf_path
```

`level` の `unknown` が最重要。走査失敗・robots 拒否・タイムアウトは `none` ではなく `unknown` にする。UI では空欄＋「取得できませんでした」と表示し、欠落件数に**数えない**。

---

## 4. 調査項目（31項目）

7分類。`criteria` テーブルに初期投入する。実在6校（首都圏・関西・東北／進学校・中堅校・大学附属校／男子校・女子校・共学校）のサイト構造調査に基づく。

| ID | 分類 | 項目 | 主に見る人 |
|---|---|---|---|
| A1 | 理念・方針 | 建学の精神・教育理念 | 保護者・塾 |
| A2 | | 校長・学園長挨拶 | 保護者 |
| A3 | | スクールポリシー（3つの方針） | 保護者・塾 |
| B1 | 教育内容 | 6年間のカリキュラム・教育課程 | 保護者 |
| B2 | | 探究学習 | 保護者・塾 |
| B3 | | グローバル教育・英語・留学 | 受験生・保護者 |
| B4 | | ICT教育・1人1台端末 | 保護者 |
| B5 | | 学習サポート・補習体制 | 保護者 |
| C1 | 学校生活 | 年間行事 | 受験生 |
| C2 | | 部活動の個別ページ | 受験生 |
| C3 | | 施設・設備 | 受験生 |
| C4 | | 制服紹介 | 受験生 |
| C5 | | 1日の流れ・時程 | 受験生 |
| C6 | | 在校生の声 | 受験生 |
| C7 | | 授業風景の写真・動画 | 受験生 |
| D1 | 進路 | 進学実績（過去3年） | 保護者・塾 |
| D2 | | 進路指導・進路サポート | 保護者 |
| D3 | | 卒業生の声 | 受験生・保護者 |
| D4 | | 系列大学への内部進学・推薦制度 | 保護者 |
| E1 | 費用・安心 | 学費（ページ本文への掲載） | 保護者 |
| E2 | | 奨学金・特待制度 | 保護者 |
| E3 | | 学校評価・安全対策 | 保護者 |
| F1 | 入試・導線 | 募集要項（一般・帰国生） | 保護者・塾 |
| F2 | | 説明会の日程・申込 | 保護者 |
| F3 | | 塾・教育関係者対象説明会 | 塾 |
| F4 | | 合同相談会・外部フェアの日程掲載 | 保護者 |
| F5 | | 入試データ（結果・出願状況・過去問） | 塾 |
| F6 | | アクセス・通学 | 保護者 |
| G1 | 情報発信 | デジタルパンフレット・資料請求 | 保護者 |
| G2 | | 公式SNS（媒体と導線） | 受験生 |
| G3 | | ブログ・お知らせ | 受験生・保護者 |

### 判定ルール（実装上の最重要ポイント）

**語句一致で判定してはならない。** 同じ内容でも学校ごとに名称が異なる。

| 項目 | 実在校で確認された名称 |
|---|---|
| 探究学習 | 探究学習／自調自考論文／総合学習／探究コース／BEYOND・理数探究・αゼミ |
| 学費 | 諸費用／学納金／入学金・学費／授業料等 |
| 施設・設備 | 施設・設備／施設紹介／施設案内／キャンパスマップ |
| 学習サポート | 学習フォロー体制／学習支援プログラム／特別講座／サポート体制 |

判定パイプライン：

1. **候補ページの抽出**（ルールベース）— URL パス・ナビゲーション階層・見出しから候補を絞る
2. **内容判定**（LLM）— 候補ページの本文を渡し、項目に該当するか、掲載量はどの水準かを判定させる
3. **根拠の保存** — 判定に使ったURL、画像点数、記事件数などを `evidence_counts` に保存

特殊ルール：

- **C6 在校生の声** — 独立ページの有無で判定しない。「生徒本人の一人称の発言がサイト内に存在するか」で判定する。実在6校のうち専用ページを持つのは1校のみで、探究・留学の紹介ページ内に埋め込む形が主流。独立ページで判定すると「あるのに、ない」と誤判定する。
- **D4 系列大学への内部進学** — `has_affiliated_university` が false の学校は `n/a`。欠落として数えない。
- **F1 募集要項** — 中学／高校／帰国生で系統が分かれる。`has_junior_admission` / `has_senior_admission` に応じて判定対象を変える。

### レベル定義

| level | 意味 |
|---|---|
| full | 詳細ページあり |
| mid | 記載はあるが浅い |
| thin | 一言のみ |
| none | 該当情報なし |
| n/a | 判定対象外（系列大学なし等） |
| unknown | 走査できなかった |

---

## 5. 画面仕様

### 01 サマリー

- ヒーロー：**「比較4校すべてが公開していて、本校にない情報 — N件」**
  - `level='none'` かつ 比較校すべてが `!='none'` かつ `!='unknown'` の項目数
  - `unknown` を含む項目はこの集計から除外する
- 本校が比較校より整っている情報（`level='full'` かつ 比較校に `!='full'` が1校以上）
- 比較校の更新記録（前回スキャンとの差分）
- 指摘件数の内訳（出典別 × 優先度・難易度）
- 優先度が高く難易度の低いアクション上位5件。**チェックボックスは対応済みトグル**で、06と状態を共有する（詳細は06の項を参照）。見た目だけの装飾要素にしないこと。

### 02 欠落マップ

- 31項目 × 6列（自校＋比較5校）のマトリクス。分類ごとに見出し行。
- マーク：● full ／ ◐ mid ／ ○ thin ／ — none ／ 空欄 n/a・unknown
- セルクリックで根拠パネル（判定理由＋使用URL）
- フィルタ：全項目／本校にない情報／4校すべてにあり本校になし／本校が上回る項目
- 判定方式の説明カード（名称ゆれの一覧）を常設

### 03 導線の実測

- 説明会申込までのクリック経路を、自校と最短校で並べて表示
- 実在6校との比較表（クリック数・トップの常設導線・学校案内を読むまで・予約方式）
- 計測値バー（測定方法タグ付き）
- 更新の実態（行事から公開までの日数・カテゴリ数・内訳・自校の露出報告）
- 測定条件と再現性（走査／操作／外部測定の3段階）

指標定義の注意：

- 「資料請求フォームまでのクリック数」は使わない。実在6校すべてがデジタルパンフレットを持ち、郵送用フォームを持たない学校もある。**「学校案内を読み始めるまでのクリック数」**で測る。
- クリック数は最短経路で数える。UI では「最短でも N クリック」と表記。
- 表示速度のみ外部測定に依存し再現性がない。明示する。

### 04 発見のされ方

この画面の役割は**認知の獲得**。02・03 が「来た人」を扱うのに対し、ここだけが「まだ知らない人」を扱う。

- SE-01 一般検索（地域名・沿線名・特色語）での自校と比較校の順位 ← **主役**
- SE-02 学校名で探されたときの検索結果プレビュー（自校 vs 比較校）
- SE-03 ページ名称と検索語のズレ（02 の名称ゆれが順位として現れる箇所）
- SE-04 設定状況の点検 — 冒頭に「先に直す5つ」、各行にアクションIDを紐付ける

技術チェックの羅列は制作会社の無料診断と同じ土俵なので主役にしない。

### 05 ペルソナ仮説

6パターン（小学6年生・中学3年生・保護者 × 男女）。保護者は「母親／父親」表記に切り替える。

- 生成物は必ず「サイトの記載内容から自動生成した仮説」と明示
- 各仮説に `findings` の criterion_id を紐付けて根拠を表示
- 検証用アンケート設問の生成ボタン

**この画面を「分析結果」として提示しないこと。** 会議で反発を招き、製品全体の信用を損なう。

### 06 改善アクション

- 優先度／難易度でグループ化を切り替え
- 出典（02/03/04/05）でフィルタ
- 各アクション：**対応済みトグル**（`actions.status`を`open`⇔`done`で切り替え）→ 根拠 → 実施内容 → 文案 → メタ情報 → 照会欄
- 照会欄は `action_threads` に保存。学校の事情を入力すると、その施策の位置づけを再評価した回答を返す
- 対応済み件数を画面上部に表示（例：「対応済み 2 / 18件」）

**対応済みトグルは01サマリーの SM-04 と状態を共有する。** 同じ `actions.id` に対する状態変更は、どちらの画面から行っても即座に他方へ反映される。プロトタイプでは単一のインメモリ状態オブジェクトで実装しているが、実装では `actions.status` の更新をバックエンドに永続化し、両画面が同じデータソースを参照する設計にする（楽観的UI更新 + PATCH）。

**所要時間・期限は出さない。** 学校ごとの体制で大きく変わり、こちらが決めてよい数字ではない。

優先度・難易度の定義：

| | 高 | 中 | 低 |
|---|---|---|---|
| 優先度 | 比較4校すべてが公開／申込導線上の離脱点 | 一部の比較校が公開／間接的に影響 | 整備済みだが改善の余地 |
| 難易度 | 委員会・管理職の決裁、または外部委託が必要 | 他分掌との調整、または新規素材の作成が必要 | 広報部の判断で完結し、素材も揃っている |

難易度を作業量ではなく**「どこまで話を通す必要があるか」**で定義している。学校で施策が止まる主因は工数ではなく合意形成のため。

### 07 レポート出力

- 含める内容をチェックで選択（比較校との情報差／本校の強み／導線／発見のされ方／ペルソナ仮説／改善アクション）
- PDF 出力、比較校名を伏せるオプション
- 末尾に必ず注記：本校内での検討目的である旨、比較校の記載は公開情報の記録であり優劣評価ではない旨、校外配布を控える旨

---

## 6. クロール仕様

- robots.txt を必ず尊重する。拒否されている学校は走査せず `scans.status='blocked'`、`findings.level='unknown'`
- クロール間隔を空け、同時接続数を制限する
- 深度は 4 を既定
- PDF は本文抽出を試み、失敗したら「PDFのみ」として記録
- **比較校のページ本文をそのまま保存・再配布しない。** 判定に必要な集計値と URL のみ保持する
- 週次で自動実行。差分を検出して 01 の「比較校の更新記録」に反映

---

## 7. 権限設計

- 組織単位（学校法人）で分離。自校のデータは組織外から見えない
- 役割：管理者（比較校の設定・レポート出力可）／閲覧者（閲覧のみ）
- 比較校として登録された学校に、登録された事実は通知されない。ただしレポートには社外秘表記を必須とする

---

## 8. Phase 分割

### Phase 1（これを作る）

- 認証（Supabase Auth）／自校・比較校の登録
- クロールと 31項目の判定
- 02 欠落マップ／03 導線の実測／06 改善アクション／07 レポート出力
- 01 サマリー

### Phase 2

- 04 発見のされ方（順位計測 API の連携が前提）
- 05 ペルソナ仮説
- 06 の照会欄
- 週次の自動走査と差分通知

### Phase 3

- 検索コンソール連携（自校の実流入）
- 説明会申込数・資料請求数の手入力と、サイト改善との突き合わせ

### 明確に対象外

- 比較校のスコアリング・優劣評価
- SNS 投稿の内容分析
- 学校サイトの直接編集機能
- 個々の教員の活動量の可視化

---

## 9. 要確定事項

実装前に決める必要がある。

**A. 判定コスト**
31項目 × 6校 = 186判定／回。週次で回すと月800判定。候補ページ抽出をルールベースで絞り、LLM に渡す本文量を制限する設計が必要。比較校は月次、自校は週次、という頻度差を付けるか。

**B. 比較校の走査範囲**
比較校も全ページ走査するのか、判定に必要なページに絞るのか。前者は負荷が5倍。後者は「掲載写真の点数」のような全体集計値が取れない。→ **比較校は判定に必要なページのみ、全体集計は自校のみ**を推奨。

**C. Playwright の要否**
学校サイトは静的 HTML が多い。Cheerio のみで足りるページが大半。Playwright は JS レンダリングが必要なページのみフォールバックで使う二段構えを推奨。

**D. 順位計測**
外部 API は学校数 × キーワード数 × 頻度で課金される。Phase 1 では見送り、Phase 2 で「一般検索キーワード5語 × 月次」程度に絞るのが現実的。

**E. 判定の再現性**
LLM 判定は同じ入力でも揺れる。前回と判定が変わった項目を検出して、変化として通知するのか、揺れとして抑制するのか。→ 前回結果を判定時のコンテキストに渡し、変更する場合は理由を出力させる方式を推奨。

---

## 10. プロトタイプのQA記録

プロトタイプは静的HTML+JSの手作業で構築したため、実装前に構文チェック・DOM参照整合性・データ整合性の機械検証を行い、発見した不整合はすべて修正済み。現在の `prototype.html` はこの状態。

**検証済み（問題なし）：**
- 全 `getElementById` 参照と実際の要素IDの対応
- GAP（31項目）・ACTS（18件）データの構造整合性、ID重複なし
- 画面遷移（ナビ・`data-goto`・パネルID・見出し辞書）の対応
- 07レポートのチェックボックスと本文セクションの対応
- ペルソナ6パターン（学年3種×性別2種）の網羅性
- HTMLタグの開閉対応

**発見して修正した不整合（実装時に同じパターンを再発させないための記録）：**

1. **参照先を消した後、参照元のコードが残っていた。** 04画面の実装をやり直した際、旧仕様の要素IDへ描画するコードが削除されず残存し、存在しない要素への書き込みで例外が発生。それより後続の描画処理が連鎖的に止まった（画面が途中までしか表示されない状態になっていた）。
   → **実装時の含意：** コンポーネントを差し替える際は、旧コンポーネントが参照していたstateやAPIレスポンスの参照が他に残っていないか、削除時に必ず検索する。Reactではコンポーネント単位で状態が閉じるため、この種の「消し忘れ」は構造的に起きにくいが、共有stateやグローバルなrefを使う場合は同じ問題が再発しうる。

2. **分類ラベルを変更した際、データ側のラベルだけ変え忘れた。** ある改善アクションの分類（出典元の画面）が、リネーム前の値のまま残り、本文の参照先と矛盾していた。UIのフィルタ選択肢に対応する値がないデータが1件、事実上フィルタで探し出せない状態になっていた。
   → **実装時の含意：** 分類値・タグ・ステータスなどの enum は、DB制約またはTypeScriptのUnion型で定義し、フリーテキストで持たない。フィルタの選択肢はenumから動的に生成し、データ側の値と選択肢が構造的にズレないようにする。

3. **画面名を変更した際、別画面にあるボタンラベルが取り残された。** 04画面のタイトルを変更した際、06画面のフィルタボタンの文言だけ旧名称のまま残っていた。
   → **実装時の含意：** 画面タイトルは一箇所（ルーティング定義やi18nリソース）に定義し、他画面から参照する形にする。文言をハードコードで複数箇所に埋め込むと、同じ修正漏れが起きる。

4. **CSSクラスをHTMLに書いたが、対応するスタイル定義を書き忘れた。** 01サマリー画面のタスクリストが、クラス名は指定されているのに一切のスタイルが定義されておらず、無装飾のまま表示されていた（チェックボックスの整列崩れ、罫線なし、余白なし）。JSのエラーにはならないため、構文チェックでは検出できなかった。
   → **実装時の含意：** Tailwindのユーティリティクラスを直接使う、またはコンポーネントとスタイルを同一ファイル（CSS Modules / styled-components 等）で管理する構成にすれば、この種の「クラスは書いたがスタイルが存在しない」は原理的に起きない。プロトタイプのようにHTMLとCSSを別々に手書きする構成でのみ発生するバグなので、実装フェーズでは再発しにくいはずだが、念のためVisual Regression Testingの導入を推奨する。

5. **見た目だけ実装し、ロジックを繋ぎ忘れた。** 01サマリーのタスクリストにチェックボックスを配置したが、`onchange`ハンドラを実装しておらず、クリックしても何も起きない状態になっていた。UIとして「押せそう」に見えるのに反応しない要素は、何もない状態より信頼を損なう。
   → **実装時の含意：** インタラクティブに見える要素（checkbox・button・toggle）を追加する際は、対応するハンドラの実装とセットでコミットする運用にする。E2Eテストで「クリック可能に見える要素は実際に状態変化を起こすか」を機械的に検査できると理想的。本ツールでは対応済み状態（`actions.status`）として実装し、01と06の両画面で同じ状態を共有する設計にした（06の項を参照）。

**この5件に共通する原因：** いずれも「一箇所を変更したとき、別の箇所への影響を人力で追いきれなかった」、または「部分的にしか実装せず、繋ぎ込みを終えなかった」というパターン。静的HTMLのプロトタイプは変更の影響範囲をコンパイラが検査してくれないため、こうした不整合が蓄積しやすい。実装フェーズでは型システム（TypeScript）とコンポーネント分割によって、同種の問題の大半は自動的に防げる設計にすること。

---

## 11. 最初に作るもの

1. Next.js プロジェクト構築 + Supabase 接続
2. 認証・組織・学校登録
3. `criteria` の初期投入（31項目）
4. クロール処理（Cheerio ベース、robots.txt 尊重）
5. 判定パイプライン（候補抽出 → LLM 判定 → 根拠保存）
6. 02 欠落マップ（プロトタイプの UI を正典とする）
7. 06 改善アクション
8. 07 レポート出力

デザインはプロトタイプに準拠。淡色ベース、罫線は細く均一、数値は等幅・桁揃え。見出しに Shippori Mincho、本文に Zen Kaku Gothic New、数値に IBM Plex Mono。
